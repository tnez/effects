export default Effects

declare namespace Effects {
  /**
   * A simple function to explore the act publishing these type interfaces.
   *
   * @param name A name to be used in addressing the salutation
   */
  function helloWorld(name: string): string
}
