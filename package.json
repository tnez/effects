{
  "scripts": {
    "ci:publish": "pnpm publish -r",
    "ci:require-changeset": "changeset status --since origin/main",
    "ci:version": "changeset version"
  },
  "dependencies": {
    "@changesets/cli": "^2.26.0",
    "turbo": "^1.8.2"
  }
}
