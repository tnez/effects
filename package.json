{
  "scripts": {
    "publish-packages": "changeset publish",
    "require-changeset": "changeset status --since origin/main",
    "version-packages": "changeset version"
  },
  "dependencies": {
    "@changesets/cli": "^2.26.0",
    "turbo": "^1.8.2"
  }
}
